{
  "name": "spotify-furigana",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "A Spicetify desktop extension that adds furigana to Japanese lyrics in Spotify for Windows.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "marketing-assets": "node scripts/generate-marketing-assets.mjs",
    "package": "npm run build && powershell -NoProfile -ExecutionPolicy Bypass -File scripts/package.ps1",
    "compile": "tsc --noEmit",
    "check:app": "node --check app/index.js",
    "test": "vitest run",
    "check": "npm run compile && npm run check:app && npm run test && npm run build"
  },
  "dependencies": {
    "kuroshiro": "^1.2.0",
    "kuroshiro-analyzer-kuromoji": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.28.2",
    "gifenc": "^1.0.3",
    "path-browserify": "^1.0.1",
    "sharp": "^0.35.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=22"
  }
}
